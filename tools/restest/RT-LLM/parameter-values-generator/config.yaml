llm:
  model_path: "model/model.gguf" # Path to the local LLM
  temperature: 0.3
  device: "cpu" # "cpu", "cuda", or "auto"
  max_tokens: 512
  n_ctx: 1536
  n_threads: 8
  n_batch: 512

generation:
  n_valid_values: 6
  n_invalid_values: 1
